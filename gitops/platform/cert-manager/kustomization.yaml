# Required even to list a single manifest: it tells ArgoCD what to apply. Without it ArgoCD
# reads the whole directory and tries to apply config.yaml as a resource, which fails.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - cert-manager-v1.21.1.yaml

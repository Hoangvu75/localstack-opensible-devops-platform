apiVersion: v1
kind: Service
metadata:
  name: disk-service-svc
  labels:
    app: disk-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 4003
      targetPort: 4003
  selector:
    app: disk-service

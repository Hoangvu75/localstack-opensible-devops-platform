apiVersion: v1
kind: Service
metadata:
  name: memory-service-svc
  labels:
    app: memory-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 4002
      targetPort: 4002
  selector:
    app: memory-service
